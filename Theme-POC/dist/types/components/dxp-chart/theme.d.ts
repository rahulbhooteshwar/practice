/** Theme class for performing operations related to styling */
export default class Theme {
    /** holds configuration for themes for chart */
    config: any;
    /** Setting theme for component */
    setTheme(spec: any): Promise<any>;
}
